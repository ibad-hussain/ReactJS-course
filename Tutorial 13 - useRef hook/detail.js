// useRef hook

// useRef hook use cases
    // 1. Create variable which persist their value/state accross re-renders
    // 2. Access a DOM element directly

// Steps for accessing DOM element
    // 1. Create a reference using useRef hook
    // 2. Link the reference to an element
    // 3. Change the element properties using reference