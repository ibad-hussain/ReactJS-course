// npm packages
    // npm i @reduxjs/toolkit
    // npm i react-redux

// Redux toolkit

// Steps
    // Create a folder(redux) and then create store (store.js) inside it
    // Import Provider and store, and then wrap <App /> component under Provider in the 'main.jsx'
    // Create a folder(features), and another folder(counter), then create a Slice (counterSlice.jsx) inside it
    // Create Reducer functions in the slice
    // Register slice as reducer in the store
    // Use reducer functions in your app and avail the functionality